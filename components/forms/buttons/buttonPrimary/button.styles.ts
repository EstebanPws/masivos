import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    btn: {
        borderRadius: 50
    },
    btnText: {
        color: '#fff'
    }
})

export default styles;;